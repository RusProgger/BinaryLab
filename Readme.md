
# BinaryLab

---

## 🇬🇧 English

BinaryLab is a simple console tool for working with binary representations of numbers.

The project allows users to input a decimal number and visualize its binary form in a clear and structured way. It is designed as a learning tool for understanding how numbers are stored and processed at the bit level.

### Features:
- Convert decimal numbers to binary representation
- Fixed-width binary output (8/16/32-bit formats)
- Clean and aligned console output
- Educational focus on bitwise operations and number systems

This project was created as part of learning low-level programming concepts in C++ and computer graphics fundamentals.

## ⚙️ Build & Run:

### Requirements:
- C++ compiler (clang++ / g++ / MSVC)
- CMake (>= 3.10)

### Build:

```bash
cmake -S . -B build
cmake --build build
```

### Run program:

```bash
build\pcInfoTool.exe
```

## 🌐 Language Support

BinaryLab supports two interface languages:

    - English
    - Russian

You can switch between languages in the program menu.

---

## 🇷🇺 Русский

BinaryLab — это простой консольный инструмент для работы с двоичным представлением чисел.

Проект позволяет пользователю вводить десятичное число и наглядно видеть его двоичное представление в удобном и структурированном виде. Он создан как обучающий инструмент для понимания того, как числа хранятся и обрабатываются на уровне битов.

### Возможности:
- Перевод десятичного числа в двоичное представление
- Фиксированная разрядность вывода (8/16/32 бита)
- Чистый и выровненный вывод в консоли
- Образовательный акцент на побитовые операции и системы счисления

Проект создан в рамках изучения низкоуровневого программирования на C++ и основ компьютерной графики.

## ⚙️ Сборка и запуск:

### Требования:

- Компилятор С++ (clang++ / g++ / MSVC)
- CMake (>= 3.10)

### Сборка:

```bash
cmake -S . -B build
cmake --build build
```

### Запуск программы:

```bash
build\pcInfoTool.exe
```

## 🌐 Поддержка языков

BinaryLab поддерживает два языка интерфейса:

    - Английский
    - Русский

Переключение языка осуществляется через меню программы.

---

## 🇺🇦 Українська

BinaryLab — це простий консольний інструмент для роботи з двійковим представленням чисел.

Проєкт дозволяє користувачу вводити десяткове число та наочно бачити його двійковий вигляд у зрозумілому та структурованому форматі. Він створений як навчальний інструмент для розуміння того, як числа зберігаються та обробляються на рівні бітів.

### Можливості:
- Перетворення десяткового числа у двійкове представлення
- Фіксована розрядність виводу (8/16/32 біти)
- Чистий та вирівняний вивід у консолі
- Навчальний акцент на побітові операції та системи числення

Проєкт створений у рамках вивчення низькорівневого програмування на C++ та основ комп’ютерної графіки.

## ⚙️ Збірка та запуск:

### Вимоги:
- Компілятор C++(clang++ / g++ / MSVC)
- CMake (>= 3.10)

### Збірка:

```bash
cmake -S . -B build
cmake --build build
```

### Запуск програми:


```bash
build\BinaryLab.exe
```

## 🌐 Підтримка мов

BinaryLab підтримує дві мови інтерфейсу:

    - Англійську
    - Російську

Перемикання мови здійснюється через меню програми.

---